{
  "name": "mag-fields-calendar",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "format": "pnpm dlx prettier -w src astro.config.mjs tsconfig.json",
    "format:check": "pnpm dlx prettier -c src astro.config.mjs tsconfig.json",
    "typecheck": "astro check",
    "check": "pnpm run format:check && pnpm run typecheck && pnpm run build"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "astro": "^5.14.1",
    "playwright": "^1.58.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@astrojs/preact": "^4.1.3",
    "preact": "^10.28.3"
  }
}
